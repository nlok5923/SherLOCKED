class FHETransactionBuilder {
    constructor(address) {
        this.address = address;
    }

    getBalance() {
        // call the API to get the decrypted balance of this.address
    }

    sendTransaction(to, amount) {
        // call the API to send a transaction
    }
}
